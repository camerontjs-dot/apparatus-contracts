# Contract E × ERS cross-repository shadow RC1 preregistration

Status: preregistered before RC1 qualification-harness implementation.

## Exact subjects

- Contract E subject: `a678c73a661853a3a704666fc6bbf29fa378948f`
  - branch: `research/contract-e-v1-rc3-target-reference-cardinality-successor-20260903`
- independent ERS consumer subject:
  `0038ae3f16894e96f9aaf45ffe3bc93a6059da2c`
  - repository: `camerontjs-dot/epistemic-research-system`
  - Draft PR: `#1`
  - preregistration predecessor:
    `1237fb1dc7cab42a2c956cde5bf8524942b9204a`
- exploratory predecessor only:
  `camerontjs-dot/apparatus-contracts#113`

The exploratory predecessor is not a qualification subject and must not be used as
the RC1 branch base.

## Preserved upstream counterexample

Released Contract D 1.0.0 does not admit the ERS-specific effect
`epistemic_audit.stage_pending_review@1`; it returns
`unknown_effect_type`.

RC1 must preserve that fact. It may use only an explicit research-local effect
extension to exercise composition. It must not modify the released Contract D
effect registry or imply that the effect has been promoted.

## Preregistered claim

Given:

1. the exact independent ERS consumer commit above;
2. the exact Contract E target-cardinality successor above;
3. the ERS principal `agent:epistemic-auditor`;
4. operation `epistemic_audit.stage_pending_review@1`;
5. a target confined to
   `20_live/epistemic-audit/pending-review/`;
6. an immutable Contract D decision identity;
7. current target pre-state observed by the ERS consumer;
8. the Contract E trusted decision and AuthorityState identities;
9. an immutable execution-intent binding generated by the ERS consumer;

then the existing Contract E machinery, plus the research-local effect
extension, will permit the exact shadow case and reject the preregistered
mutations without performing execution.

## Required cross-repository evidence

The RC1 harness must load the ERS consumer from an exact checkout whose HEAD is
`0038ae3f16894e96f9aaf45ffe3bc93a6059da2c`. A different ERS HEAD is a
qualification failure.

For the exact case it must demonstrate:

- ERS derives the pending-review target;
- ERS observes the target pre-state;
- ERS builds the execution intent;
- ERS intent canonical bytes/identity and target reference are accepted
  unchanged by Contract E;
- Contract E permits the exact shadow request;
- ERS consumes that exact Contract E result successfully;
- `execution_occurred=false`;
- no MainFrame state is mutated.

## Required falsifiers

At minimum RC1 must reject:

1. wrong principal;
2. wrong execution target / target reference;
3. swapped Contract D decision identity;
4. stale authority;
5. revoked authority;
6. replay of a historical authorized result after revocation;
7. target pre-state changed after authorization;
8. generic knowledge-operation authority substituted for the ERS operation;
9. mismatched execution-intent identity;
10. a non-exact ERS consumer commit.

The existing Contract E target-reference-cardinality successor controls must
also remain green.

## Required controls

- frozen Contract E successor suite passes on the RC1 branch;
- the independent ERS commit remains unchanged;
- no real ERS pending-review write occurs;
- no `10_knowledge/` write occurs;
- no `status: stable` path is exercised;
- no released Contract D/E normative registry or schema is changed;
- `git diff --check` passes.

The historical broad integration `MATRIX-SIZE` failure observed as
`112 < 120` on the exact Contract E subject is a preserved upstream
deviation. RC1 must not repair or reinterpret it unless separately authorized.

## Falsification rules

Disposition is falsified if:

- Contract E and the exact ERS consumer disagree on the execution-intent or
  target-reference identity;
- the exact case requires rewriting ERS intent state before Contract E accepts
  it;
- any preregistered negative is permitted;
- the ERS consumer accepts a denied, mismatched, or already-executed result;
- any real MainFrame mutation is required to make the test pass;
- qualification requires changing released Contract D effect semantics.

## Non-claims

A passing RC1 does not establish:

- production Contract E readiness;
- production registration of the ERS effect in Contract D;
- authenticated principal/workload identity;
- authenticated AuthorityState/config origin;
- a production threat model;
- a real executor;
- production retry/recovery/rollback;
- permission to mutate `10_knowledge/` or assign `status: stable`;
- merge, release, promotion, or operational authorization.
