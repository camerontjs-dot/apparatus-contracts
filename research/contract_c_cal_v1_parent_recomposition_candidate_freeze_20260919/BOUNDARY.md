# Frozen Contract C CAL V1 parent-recomposition candidate

This directory freezes an exact downstream-qualification subject. It does not create an official Contract C release or assign a public version.

The frozen implementation is the unchanged producer candidate at blob `df6b6ed410f52cafaeadfe1578d770f480a34b09`, source commit `7138f6c1599b9b8f8c655f85c9eba0c264fb3b26`.

The candidate is bounded to the exact frozen CAL V1 PIPE01–PIPE04 cohort and combines:

- unchanged Candidate A RC2 child-result grammar;
- one typed parent recomposition binding for declared exact `all_of`;
- exact child native-result identities and conclusions;
- exact decomposition-receipt identity;
- exact frozen CAL semantic and resolver authority;
- exact whole-object authority.

Producer-side evidence is preserved in Apparatus PR #118. Fresh downstream consumption is independently supported by research-scaffold-harness PR #42 and decisive run `35461341449`.

The earlier RC0 clean-room aperture failure remains valid historical evidence and is not overwritten by this freeze.

## What this freeze authorizes

Only downstream qualification against these exact frozen bytes and pinned authority.

It does not authorize:

- Contract C version assignment or release;
- canonical discovery changes;
- reinterpretation or widening of RC2 semantics;
- Decision Engine source changes;
- Contract D changes;
- Authorization or execution.

Any downstream qualification must identify this freeze exactly and must preserve a failure if the current consumer cannot accept it without changing its own qualified semantics.
