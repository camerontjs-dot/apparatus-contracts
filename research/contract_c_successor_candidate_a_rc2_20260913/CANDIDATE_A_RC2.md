# Contract C successor Candidate A RC2 research contract

**Classification:** Draft Research / Research Infrastructure. This is not a production Contract C, release, SemVer assignment, CAL production change, Decision Engine production change, Contract E / Authorization surface, or execution authority.

**Exact predecessor:** Candidate A RC1 freeze `ba5f55b0fb6ca54e0461a688813b66fd81bf8c4c`.

**Falsifier authority:** CAL Draft PR #107, terminal `FALSIFIED`, decisive evaluation SHA-256 `63bc3d46b8762d51c9c07ca71572d68175588e216f6b24e93660cc43db9f88eb`.

## Research profile

`contract-c-successor-candidate-a-rc2-research`

RC2 preserves the Candidate A architecture from RC1: canonical minimal sufficient basis groups over exact public evidence participants. It does not reopen the Phase 2 representation decision or restore any Phase-1.5-ablated Contract C 1.0 surface.

## Exact semantic delta from RC1

RC2 changes only:

1. the research profile identity from RC1 to RC2;
2. the closed public terminal-reason vocabulary by adding exact case-sensitive `UNSUPPORTED_SEMANTIC_FAMILY`;
3. coherence rules for that new reason.

For `UNSUPPORTED_SEMANTIC_FAMILY`:

- proposition execution is `completed`;
- completion is `not_checkable`;
- terminal verdict is `not_checkable`;
- `basis_groups` is empty;
- every retained participant is `non_polarized / residual`.

This records CAL's explicit fail-closed result when the proposition semantic family is outside the current V1 deciding scope. It is not a causal-evidence claim and therefore has no causal basis.

## Distinction from no-deciding

`UNSUPPORTED_SEMANTIC_FAMILY` and `no_deciding_relation` are distinct public states even when their retained participant/basis shape is currently identical.

`no_deciding_relation` means a supported semantic-family path completed without a deciding relation. `UNSUPPORTED_SEMANTIC_FAMILY` means CAL refused the semantic family itself under the frozen V1 boundary. Neither may be inferred from the other, case-folded, or normalized to the same terminal reason.

Changing only this reason changes canonical object bytes, local result identity and whole-object hash.

## Inherited RC1 obligations

Unchanged from exact RC1:

- exact Contract-B world binding;
- exact CAL semantic implementation, policy digest and immutable policy-resolver binding;
- exact proposition ID and content hash;
- result-set and proposition execution separation;
- participant exact `(source_id, passage_id)` identity;
- participant relation vocabulary `supports | refutes | non_polarized`;
- independent `causal | residual` role;
- canonical minimal sufficient `basis_groups`;
- causal coverage/minimality rules;
- stable terminal semantics for support, refutation, mixed, unresolved, joint-public-cause and no-deciding;
- canonical permutation handling;
- content-derived local identity;
- independently established whole-object authority;
- exact-profile unknown-field rejection;
- exclusion of destination policy, Authorization, execution occurrence, old generic assessment slots, public rule/state causal namespaces, repeated policy payload, repeated passage hash, contribution IDs and measurements/scores.

## No architecture reopening

CAL #107 established within the tested producer fragment that basis groups can be deterministically materialized from existing CAL relation/composition authority using unchanged `compose()` only, while the old flat causal strategy is discriminated and false. RC2 therefore does not alter basis-group semantics to repair a terminal-vocabulary omission.

## Nonclaims

RC2 does not establish CAL producer conformance, independent-consumer conformance, universal semantic-family coverage, a production Contract C version, SemVer class, Decision policy, Contract E / Authorization, or execution authority. Qualification can at most make this exact RC2 eligible for a fresh CAL producer-conformance experiment.