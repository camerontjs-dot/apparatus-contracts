# Contract C successor Candidate A RC2 preregistration

**Classification:** Draft Research / Research Infrastructure. This is not production Contract C authority, SemVer assignment, release, promotion, CAL/Decision production change, Contract E / Authorization, or execution authority.

## Exact predecessor authorities

Candidate A RC1:

- architecture / qualification PR: Apparatus Draft PR #95
- exact RC1 freeze: `ba5f55b0fb6ca54e0461a688813b66fd81bf8c4c`
- RC1 terminal qualification: `QUALIFIED_FOR_CAL_PRODUCER_CONFORMANCE_RESEARCH`
- RC1 contract blob: `0ab62b7ceea1d56d6bc3b7cf8528769dd440ab7c`
- RC1 schema blob: `0de83032e6d989225cf014d9a70035f634a5e003`
- RC1 validator blob: `03dbb5b774af523d8e6235bca86b18af5ddad5b0`

CAL producer falsifier:

- CAL Draft PR #107
- exact frozen CAL authority: `a902621e8baea3063dddd7f92ba975aade305464`
- decisive run `34799018227`, job `103837717822`
- artifact `10330722658`
- artifact digest `sha256:595297587f9b1d193b6c21e2b3578bc114c75bea6ee3c157663fe56815a962ba`
- extracted evaluation SHA-256 `63bc3d46b8762d51c9c07ca71572d68175588e216f6b24e93660cc43db9f88eb`
- scientific disposition: `FALSIFIED`

The #107 evidence specifically supports minimal sufficient basis groups as deterministic compose-only materialization from current CAL relation state while falsifying RC1 completeness because its terminal-reason vocabulary cannot preserve `UNSUPPORTED_SEMANTIC_FAMILY`.

## Research question

Does the smallest successor delta from exact Candidate A RC1 safely preserve CAL's legitimate `NOT_CHECKABLE / UNSUPPORTED_SEMANTIC_FAMILY` public result without weakening, reinterpreting or reopening the already-qualified Candidate A causal architecture?

## Exact proposed semantic delta

Relative to exact RC1, RC2 may change only:

1. research profile identity: `contract-c-successor-candidate-a-rc2-research`;
2. terminal public-reason vocabulary: add exact case-sensitive `UNSUPPORTED_SEMANTIC_FAMILY`;
3. terminal coherence for that reason: it is permitted only with:
   - completed proposition execution;
   - `completion = not_checkable`;
   - `verdict = not_checkable`;
   - empty `basis_groups`;
   - every retained participant `relation = non_polarized` and `role = residual`.

No other public field, causal representation, participant relation/role vocabulary, Contract-B binding, producer/policy binding, proposition binding, execution model, canonicalization rule, local identity rule, whole-object authority rule, or Phase-1.5 ablation may change.

## Interpretation boundary

`UNSUPPORTED_SEMANTIC_FAMILY` is **not** synonymous with `no_deciding_relation`.

- `no_deciding_relation`: CAL attempted a supported semantic family path but retained no deciding causal relation for the proposition.
- `UNSUPPORTED_SEMANTIC_FAMILY`: the exact proposition semantic family is outside the current CAL V1 deciding scope and CAL fails closed without invoking legacy fallback.

The two states may currently have the same empty causal basis and residual non-polarized participant shape, but their public reason identity is behaviorally and audit-relevant state and must remain distinct.

## Positive controls

1. Exact RC2 accepts a faithful unsupported-family object with the structure above.
2. Exact RC1 rejects the same object solely because the public reason is outside RC1 vocabulary.
3. All 19 frozen Phase 2 SP/EX semantic specimens remain reconstructable under RC2 with no semantic change other than the research profile identity.
4. Existing `no_deciding_relation` remains valid and distinct.
5. `UNSUPPORTED_SEMANTIC_FAMILY` and `no_deciding_relation` produce different deterministic local identities and different whole-object hashes for otherwise matched public structure.
6. Existing `MIXED_RELATIONS` exact case-sensitive behavior remains unchanged.
7. Canonical permutation behavior remains unchanged.
8. Exact Contract-B, producer/policy/resolver, proposition-content and external whole-object authority controls remain unchanged.

## Decisive falsifiers

- F1: RC2 accepts `UNSUPPORTED_SEMANTIC_FAMILY` with a non-`not_checkable` verdict/completion.
- F2: RC2 accepts a causal basis under `UNSUPPORTED_SEMANTIC_FAMILY`.
- F3: RC2 accepts any support/refute or causal participant under `UNSUPPORTED_SEMANTIC_FAMILY`.
- F4: RC2 normalizes or aliases `UNSUPPORTED_SEMANTIC_FAMILY` to `no_deciding_relation` such that local identity or whole-object bytes become equal.
- F5: RC2 accepts lowercase/case-mutated `unsupported_semantic_family`.
- F6: the narrow repair changes reconstruction of any frozen Phase 2 SP/EX specimen.
- F7: the repair weakens exact-profile, unknown-field, reference, policy-resolution, proposition-binding, local identity or external-authority failure behavior.
- F8: Candidate A RC1 unexpectedly accepts the exact unsupported-family object, invalidating the claimed discriminator.

## Evaluator independence / sensitivity

The qualification must consume the exact frozen Phase 2 oracle directly for inherited semantics and must include a weak control that intentionally aliases unsupported-family refusal to `no_deciding_relation`. The evaluator must kill that weak control by exact terminal reason and identity comparison.

## Dispositions

- `QUALIFIED_FOR_CAL_PRODUCER_CONFORMANCE_RESEARCH`: all positive controls and falsifiers pass for exact RC2. This is not production promotion.
- `FALSIFIED_REQUIRES_SUCCESSOR_CANDIDATE`: any decisive semantic/integrity falsifier fires while apparatus remains valid.
- `INCONCLUSIVE`: frozen authority/evaluator/apparatus cannot discriminate safely.
- `SUPERSEDED`: an exact authority changes before disposition.

Preserve RC1 and CAL #107 unchanged. Do not repair either frozen record in place.