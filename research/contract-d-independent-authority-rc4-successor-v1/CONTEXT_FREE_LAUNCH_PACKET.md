# CONTEXT-FREE REQUIRED

## Contract D RC4 — Fresh Independent Consumption Reproduction, Successor v1

Use this packet as the complete task input.

Do not import, retrieve, infer from, or ask for surrounding CAL Pipeline conversation, prior Contract D experiments, prior reproduction reasoning, prior results, user memory, summaries, project history, PR narrative, workflow narrative, or other answer-bearing material. Information isolation is part of the validity claim.

Do not open project governance attachments or other project files. This packet is intentionally self-contained so the clean-room execution has fewer setup steps and fewer accidental-exposure surfaces.

The purpose is to discover whether the frozen Contract D RC4 public authority is independently recoverable and natively consumable. Do not try to make the candidate pass. Preserve failures, disagreements, ambiguity, counterexamples, evaluator weakness, native incompatibility, contamination, and inconclusive results.

Continue through routine authorized work without asking the operator for approval between tests. Escalate only for a genuine decision outside this packet.

---

## 1. Objective

Independently implement and test Contract D RC4 consumption using only the exact pre-freeze public authority below.

Determine whether a competent implementation created from that public authority alone:

1. agrees with the frozen apparatus reference on all authority-relevant behavior, including finite-JSON ingress and decoded-host-value handling;
2. meaningfully discriminates weak consumers; and
3. natively consumes the frozen Decision Engine RC4 producer objects without a bespoke translation adapter.

This run is research only. It does not authorize production changes, release, operational Authorization changes, execution, or downstream actions.

---

## 2. Frozen apparatus candidate

Repository:

`camerontjs-dot/apparatus-contracts`

Frozen candidate commit:

`fd6923115116b0ced0f9feb5c005099d2e51ea88`

Frozen root tree:

`a9ac54a04781c8d8f7aac59407a4cc3c4ab38e02`

Frozen candidate subtree:

`fe449f9ec27eeddb434276ded375f9dc16b48e29`

Fixture corpus tree:

`3977ba5b9a92c8b4aa10385408ec59cd7d8ec2a0`

Contract D version token:

`0.3.0-rc4`

Pre-freeze access to this repository must be exact-path and exact-ref constrained. Do not browse the branch generally, repository history, adjacent commits, PRs, issues, workflows, action logs, search results, sibling research directories, or other branches.

The supplied immutable identities are authority. Do not fetch broad commit/tree endpoints merely for orientation.

---

## 3. Mandatory isolation preflight

Do this before opening any Contract D candidate content.

Implementation repository:

`camerontjs-dot/research-scaffold-harness`

Clean starting commit:

`548bfa81f65290eda15af658f647497679b840ef`

Authorized pre-created successor branch:

`research/contract-d-rc4-fresh-reproduction-v2`

Using a ref-only Git surface, verify:

`refs/heads/research/contract-d-rc4-fresh-reproduction-v2`

points exactly to:

`548bfa81f65290eda15af658f647497679b840ef`

If equal: proceed. Do not create another branch.

If not equal: STOP without inspecting that branch's commits, tree, files, diff, history, PR, or workflow results. Record primary `INCONCLUSIVE`, secondary `BLOCKED`, reason `AUTHORIZED_SUCCESSOR_SURFACE_NO_LONGER_AT_CLEAN_BASE`.

Do not inspect any earlier or sibling Contract D reproduction branch before freeze, including `research/contract-d-rc4-fresh-reproduction` or any RC3 reproduction branch.

---

## 4. Mandatory pre-freeze access ledger

Before opening the first allowlisted candidate file, create:

`research/contract-d-rc4-fresh-reproduction-v2/PRE_FREEZE_ACCESS_LOG.md`

Record every material source opened before freeze with sequence number, repository/local source, exact path/surface, exact ref if applicable, expected blob/identity if applicable, observed blob/identity, source class, and purpose.

Record observable access facts only. Do not include hidden chain-of-thought.

If a tool unexpectedly exposes answer-bearing material outside the allowlist, record the exposure and apply the contamination stop rule.

---

## 5. Exact pre-freeze allowlist

At `camerontjs-dot/apparatus-contracts@fd6923115116b0ced0f9feb5c005099d2e51ea88`, open only these Contract D authority files before the independent freeze:

1. `research/contract-d-independent-authority-rc4/candidate/SPEC.md`
   expected blob: `42a9819651ab41efdb154240eab4f7d808887cd6`

2. `research/contract-d-independent-authority-rc4/candidate/schema.json`
   expected blob: `b17183038b75f3ee00804e63c2d9b8d7da476f2e`

3. `research/contract-d-independent-authority-rc4/candidate/effect-registry.json`
   expected blob: `53df222ca439248a44029e02a662825235db892f`

4. `research/contract-d-independent-authority-rc4/candidate/fixtures/valid.json`
   expected blob: `f40364a4b0a4e02e60fc08f8d0038ad0cb531e58`

5. `research/contract-d-independent-authority-rc4/candidate/fixtures/invalid.json`
   expected blob: `74ec69e79c8299d7e9d9ade6e19ee5a42424a7fc`

6. `research/contract-d-independent-authority-rc4/candidate/conformance-cases.json`
   expected blob: `29825bfa89b2b91bfa9e457c001e2c869a3649a4`

Verify every observed blob identity. On any mismatch, STOP without searching for a replacement. Record primary `INCONCLUSIVE`, secondary `BLOCKED`, reason `FROZEN_AUTHORITY_IDENTITY_MISMATCH`.

If the public authority appears ambiguous, implement the best defensible interpretation and record the ambiguity before reveal. Do not seek the hidden answer.

---

## 6. Pre-freeze denylist

Before the independent freeze do not inspect, search, retrieve, summarize, or use:

### Apparatus hidden/reference material

- `research/contract-d-independent-authority-rc4/candidate/contract_d_core.py`
- `research/contract-d-independent-authority-rc4/candidate/contract_d_validate.py`
- `research/contract-d-independent-authority-rc4/candidate/contract_d_consume.py`
- `research/contract-d-independent-authority-rc4/candidate/tests/test_rc4.py`
- `research/contract-d-independent-authority-rc4-successor-v1/APPARATUS_FREEZE_RECEIPT.md`
- any prior RC4 launch packet, freeze receipt, final record, comparison, result, PR, issue, workflow/check result, review, discussion, or correspondence

### Prior Contract D material

Do not inspect prior independent implementations, prior clean-room branches, predictions, tests, results, comparisons, receipts, launch packets, workflow outputs, generated artifacts, RC3 reference material, or predecessor diagnostic history.

### Decision Engine before Phase B

Do not inspect the RC4 native producer before the independent freeze and Phase A comparison are complete.

Do not inspect older Contract D producer implementations or outputs.

### Conversation, memory, search, project files

Do not use prior conversations, summaries, user memory, assistant memory, personal-context retrieval, project-context retrieval, web search, GitHub code search, issue/PR search, repository-wide search, search snippets, or attached/project governance files for Contract D.

Knowing filenames and immutable identities printed in this packet is not contamination. Opening denied answer-bearing contents is.

---

## 7. Isolated implementation target

Work only under:

`research/contract-d-rc4-fresh-reproduction-v2/`

on branch:

`research/contract-d-rc4-fresh-reproduction-v2`

Do not inspect other branches for implementation patterns. Do not copy prior Contract D code.

Prefer a self-contained implementation and standard-library tests. Do not inspect repository-wide CI/workflow/config to look for a hosted runner. Deterministic local tests are sufficient for this experiment. In the freeze receipt record:

`HOSTED TEST: NOT REQUESTED BY LAUNCH PACKET`

This removes hosted-runner discovery as a clean-room setup step.

---

## 8. Required independent implementation

Using only the six public authority files, independently implement:

- exact-version parsing;
- validation and unknown-field behavior;
- genuine finite-JSON ingress validation;
- decoded-host-value validation;
- typed effect registry consumption;
- stored effect parameter validation and safe-default normalization;
- external requested-effect-parameter constraint semantics;
- deterministic canonicalization;
- semantic authority projection;
- semantic Decision identity;
- downstream applicability over expected upstream authority, policy, target, external requested operation, and requested machine-semantic effect parameters;
- completed CLEAR, completed HOLD, and evaluation failure as distinct outcomes.

Do not delegate interpretation to the hidden reference.

---

## 9. Required prereveal tests

Do not limit testing to public fixtures.

Generate mutation, metamorphic, replay, substitution, invariance, parser, finite-JSON, and weak-consumer tests.

At minimum cover:

### State controls

- source-audit CLEAR
- citation-use CLEAR
- task-dispatch CLEAR
- completed HOLD
- evaluation failure
- HOLD distinct from failure

### Authority sensitivity

Mutate and classify effects on validity, semantic identity, and applicability for:

- Contract D version
- upstream kind/id/immutable id
- policy id/version
- target kind/id/immutable content
- evaluation state
- disposition
- effect type/version
- every machine-semantic effect parameter

### Requested-operation / parameter discriminators

Test prereveal:

1. completed HOLD with matching upstream/policy/target but different requested operation -> expected public-authority behavior;
2. completed HOLD with conflicting explicitly requested effect parameter;
3. completed HOLD with exact requested operation/parameter applicability;
4. CLEAR Decision normalized to `{"scope":"object"}` with requested params absent;
5. same Decision with requested params `{}`;
6. same Decision with requested `{"scope":"claim"}`;
7. same Decision with requested `{"scope":"object"}`.

### Finite-JSON discriminators

Test prereveal:

- invalid UTF-8 JSON bytes;
- duplicate JSON object keys;
- non-finite JSON numbers;
- host-language-only diagnostics values;
- non-string object keys in decoded objects;
- self-referential decoded list/dict containers;
- mutually recursive decoded containers;
- shared-but-acyclic decoded containers, to distinguish cycle rejection from blanket repeated-identity rejection.

Derive expected outcomes from the public authority and freeze them in predictions before reveal.

### Authority invariance

Mutate/remove metadata reason codes, explanation, and diagnostics and demonstrate semantic-identity/applicability invariance.

Demonstrate Authorization-only context such as actor, approval, delegation, autonomy, profile/trust state, execution permission/state/receipt does not modify Contract D Decision semantic identity. Do not insert those fields into Decision except as negative injection tests or opaque diagnostics.

### Replay/substitution

Test requested-operation replay; target id/kind/content substitution; upstream substitution; policy id/version substitution.

### Future/unknown behavior

Test unknown contract version, evaluation state, disposition, effect type, effect version, effect parameter, and structural field. No future/unknown value may gain current authority.

### Canonicalization/identity

Test key ordering, whitespace/formatting, duplicate keys, Unicode, safe-default normalization, metadata mutation, and authority-bearing mutation. Distinguish validity, canonical bytes, semantic projection, and semantic identity.

### Weak-consumer discrimination

Construct and reject at minimum:

- CLEAR/disposition-only
- target-id-only
- target consumer ignoring kind/content
- HOLD/failure collapse
- reason-text effect inference
- unknown-effect acceptance
- policy-blind
- upstream-blind
- omitted requested params treated as registry-default request constraints
- HOLD returned before operation/parameter applicability
- host-language-only diagnostics acceptance
- cyclic decoded-container acceptance or uncontrolled recursion
- Decision identity contaminated by Authorization context

If a materially weak consumer passes the same decisive gate, record evaluator weakness. Do not silently strengthen after seeing reference behavior and claim the original evaluator was adequate.

---

## 10. Required prereveal predictions

Before opening any denied reference material create:

`research/contract-d-rc4-fresh-reproduction-v2/PREDICTIONS.md`

Record expected behavior for:

- every public conformance case;
- major mutation classes;
- unknown/future cases;
- HOLD versus failure;
- semantic-identity invariants;
- applicability mismatches;
- every requested-operation/parameter discriminator above;
- every finite-JSON discriminator above;
- every weak-consumer control.

Do not guess incidental reference error strings.

---

## 11. Independent freeze

Before reference reveal, freeze:

- implementation;
- tests;
- self-generated fixtures/cases;
- weak consumers;
- `PRE_FREEZE_ACCESS_LOG.md`;
- `PREDICTIONS.md`;
- any local configuration required to reproduce the suite.

Create and push one Git commit on:

`research/contract-d-rc4-fresh-reproduction-v2`

This is the independent freeze commit.

Create an additive pre-reveal freeze receipt recording:

- clean base `548bfa81f65290eda15af658f647497679b840ef`;
- branch;
- freeze commit and tree;
- implementation/test/weak-consumer/prediction/access-log blob identities;
- self-generated case/fixture tree identities;
- exact public authority sources opened and observed blobs;
- denied surfaces not opened;
- exact local test command/result;
- `HOSTED TEST: NOT REQUESTED BY LAUNCH PACKET`;
- deviations;
- timestamp/order proving freeze precedes reveal.

After reveal, do not modify any frozen implementation, test, case, weak consumer, access-log, or prediction file. Post-reveal machinery must be additive.

---

## 12. Contamination stop rule

If any denied answer-bearing material is exposed before independent freeze:

- stop the independence claim;
- record exactly what, how, and when;
- preserve partial artifacts but do not treat them as independent evidence;
- do not attempt to unsee the material or restart within this task.

Use primary `INCONCLUSIVE` and secondary `CONTAMINATED` unless the exposure independently establishes another permitted primary disposition.

---

## 13. Post-freeze reveal

Only after the independent freeze and additive freeze receipt exist.

### Phase A — frozen apparatus reference

At `camerontjs-dot/apparatus-contracts@fd6923115116b0ced0f9feb5c005099d2e51ea88`, reveal in this order:

1. `research/contract-d-independent-authority-rc4/candidate/contract_d_core.py`
   expected blob: `589e3f1c31a21d305402e5750605d25be682a336`

2. `research/contract-d-independent-authority-rc4/candidate/contract_d_validate.py`
   expected blob: `d9d621df1e817adbb5468be25ef65272c457e8cc`

3. `research/contract-d-independent-authority-rc4/candidate/contract_d_consume.py`
   expected blob: `ad5126922ea4dd8a38df6c08f53e3bc687f2c4d4`

4. `research/contract-d-independent-authority-rc4/candidate/tests/test_rc4.py`
   expected blob: `8bece62cc9d4734af0f6ebee75bb39a0221ce397`

Verify every blob. Compare without editing the frozen independent object.

Run the frozen reference tests using only the revealed reference files plus the already-authorized public authority files. Record all authority-relevant agreements/disagreements. Do not count internal architecture, names, incidental error strings, or non-normative formatting as disagreements.

Durably record the Phase A comparison before Phase B.

### Phase B — native Decision Engine RC4 producer

Only after Phase A comparison is durably captured, reveal:

Repository:

`camerontjs-dot/decision-engine`

Commit:

`e768cedc891fa0d3280dc55f54b578d149019555`

Path:

`research/contract-d-rc4-producer-conformance/emit.mjs`

Expected blob:

`96d7856493c498080e3e34366654aeebd14db9f4`

Execute the producer unchanged and supply its RC4 Decision objects directly to the frozen independent consumer.

Do not add translation, compatibility adapter, shape normalizer, field rename, version bridge, effect mapper, or default injector not required by the frozen independent contract.

Decisive path:

`Decision Engine -> frozen RC4 object -> frozen independent consumer`

Test the three CLEAR classes, completed HOLD, and evaluation failure. Preserve the first native result.

A diagnostic translation may be used only afterward and must be explicitly labeled non-conformant comparison machinery.

Do not open older Decision Engine or Contract D material for historical diagnosis unless a specific unresolved disagreement cannot otherwise be classified and the evidence would change the terminal disposition.

---

## 14. Success criterion

`SUPPORTED FOR PROMOTION` requires all of:

1. frozen independent implementation agrees with frozen reference on all authority-relevant RC4 semantics, including finite-JSON/decoded-host-value behavior;
2. evaluator meaningfully discriminates the required weak consumers;
3. no unresolved authority-relevant disagreement remains;
4. native Decision Engine -> RC4 -> frozen independent consumer succeeds without a bespoke adapter.

A green suite alone is insufficient.

This run never itself authorizes production release or execution.

---

## 15. Explicit falsifiers

Preserve as potentially decisive:

- unknown/future values gaining authority;
- HOLD collapsed with failure;
- HOLD returned despite requested-operation or requested-parameter mismatch;
- omitted external requested params silently converted into registry-default constraints;
- target kind/id/content replay weakness;
- upstream or policy substitution accepted;
- machine-semantic effect parameters ignored;
- Authorization context changing Decision identity;
- reason/explanation becoming authority;
- canonicalization disagreement changing semantic identity;
- invalid UTF-8 escaping fail-closed behavior;
- duplicate keys accepted;
- non-finite or host-language-only values accepted as Contract D JSON data;
- cyclic decoded containers accepted, hang, overflow, or escape without the Contract D fail-closed outcome;
- shared acyclic containers rejected solely because object identity repeats;
- valid/invalid disagreement;
- effect-registry/default disagreement;
- native object requiring translation;
- weak consumer passing the decisive evaluator;
- public authority admitting multiple unresolved authority-relevant interpretations.

Classify disagreements as:

- independent implementation defect under explicit authority;
- reference implementation defect under explicit authority;
- specification underspecification;
- native producer nonconformance;
- authority-relevant semantic disagreement;
- non-authority/incidental variance;
- evaluator weakness;
- unresolved/unknown.

Do not force classification when evidence cannot discriminate.

---

## 16. Terminal dispositions

Use exactly one primary:

- `SUPPORTED FOR PROMOTION`
- `FALSIFIED`
- `INCONCLUSIVE`
- `SUPERSEDED`

Use secondary labels as justified:

- `INDEPENDENT_REPRODUCTION_SUCCEEDED`
- `CROSS_REPOSITORY_CONFORMANCE_FAILED`
- `CONTRACT_AUTHORITY_STILL_UNDERSPECIFIED`
- `REFERENCE_IMPLEMENTATION_DEFECT`
- `CONTAMINATED`
- `BLOCKED`

---

## 17. Required final record

Return a durable final record containing:

- isolation repository/branch/base/ref-only result;
- complete pre-freeze aperture and observed blobs;
- independent freeze commit/tree/file identities;
- exact local test command/results;
- hosted statement required by this packet;
- exact contamination statement: `NO PRE-FREEZE DENIED MATERIAL OBSERVED` or `CONTAMINATED`;
- every post-freeze revealed source/ref/path/blob/reveal order;
- Phase A authority agreement/disagreement table;
- native producer-consumer results for three CLEAR, HOLD, failure, and whether translation was required;
- evaluator assurance and weak-control outcomes;
- falsifiers: triggered / not triggered / not evaluated;
- primary disposition and secondary labels;
- explicit non-claims covering production authorization, release authorization, Authorization correctness, execution correctness, claims outside tested RC4 authority, and untested compatibility surfaces;
- smallest justified next step only.

End as one of:

- `TERMINAL`
- `BLOCKED`
- `OPERATOR DECISION REQUIRED`
- `NEW EXPERIMENT REQUIRED`

Use `OPERATOR DECISION REQUIRED` only for a real choice this packet cannot resolve. Otherwise continue the authorized experiment to its smallest useful terminal point.

## Final anti-goal

Do not optimize for "Can I make RC4 pass?"

Optimize for "What does this frozen independent experiment actually establish, and what could still explain the evidence?"

A clean negative result is more valuable than a contaminated positive one.
