# Candidate A RC1 adversarial qualification preregistration

**Classification:** Draft Research / Research Infrastructure.

**Frozen candidate:** `ba5f55b0fb6ca54e0461a688813b66fd81bf8c4c`.

This preregistration follows the RC1 candidate freeze and precedes the decisive RC1 evaluator/result. The three candidate blobs may not change after attack reveal. A required candidate repair falsifies RC1 and belongs to a successor candidate.

## Qualification question

Does exact frozen RC1 preserve the frozen Phase 0–2 public semantics and bindings, including verbatim stable terminal reason identity, while canonicalizing set-like structure deterministically and detecting the preregistered semantic substitutions under the complete verifier stack strongly enough to justify separate CAL producer-conformance research?

A PASS does not mean candidate bytes prove empirical causal truth. Claimed minimal sufficiency remains a producer-conformance obligation.

## Mandatory oracle anchoring

Before the cohort executes, the workflow must prove these repository blob identities:

- Phase 1 `SEMANTIC_SPECIMEN_CORPUS.md` blob `0ff4f66bca00924755d42ed7f944c4dae8d10f66` at programme freeze `175246ae16932f2f34a399560d7f76013213bf97`;
- Phase 2 `phase2_bakeoff.py` blob `9f00cd332ad366b878f60d13440af639360928de` inherited at `f5337dedaad045aa290e80f69dc83ac1a0f73436`;
- RC1 doc/schema/validator blobs `0ab62b7ceea1d56d6bc3b7cf8528769dd440ab7c`, `0de83032e6d989225cf014d9a70035f634a5e003`, and `03dbb5b774af523d8e6235bca86b18af5ddad5b0`.

The evaluator must import/consume the frozen Phase 2 oracle directly. It may translate representation vocabulary (`support` to `supports`, `refute` to `refutes`) and add the already-frozen immutable policy-resolver binding needed by RC1, but it may not copy, normalize, case-fold, or independently restate terminal reason values.

## Positive cohort

Encode and reconstruct all 19 inherited Phase 2 semantic/execution specimens directly from the frozen Phase 2 oracle: `SP-01` through `SP-14`, `EX-01` through `EX-05`.

Required equality is exact after the representation-only projection described above. In particular, SP-09 through SP-12 must retain `MIXED_RELATIONS` verbatim.

Each positive specimen must validate, reconstruct the exact frozen semantics, resolve participant refs against the exact bound B index, resolve the implementation/policy pair through the independently selected immutable resolver authority, and preserve its independently recorded full-object handoff digest.

## Canonical equality controls

Pure permutations of semantically unordered structure must preserve canonical bytes, local result identity, and full-object digest:

1. participant order;
2. basis-group order;
3. member order inside a joint group;
4. proposition order;
5. combined permutation.

## Decisive attack matrix

RC1 retains the 36 RC0 preregistered attack classes and adds the falsifier that RC0 missed. Every mutation must be detected by the appropriate structural/semantic gate, exact-B gate, immutable producer/policy resolver gate, independent whole-object authority, and/or exact frozen oracle comparison. A semantically changed coherent reseal is allowed to be a locally valid different object, but must never be silently equal to the authorized frozen specimen.

1. wrong research profile;
2. unknown top-level field / profile self-extension;
3. destination threshold/routing injection;
4. actor/delegation/Authorization injection;
5. old generic assessment-slot injection;
6. repeated full policy-payload injection;
7. repeated `passage_sha256` injection;
8. separate `contribution_id` injection;
9. duplicate exact participant;
10. duplicate equivalent basis group using member permutation;
11. duplicate member inside one basis group;
12. residual participant inserted into a basis group;
13. causal participant left uncovered by the basis family;
14. represented non-minimal strict-superset group;
15. fake one-group joint `{S1,S2}` over frozen independent alternatives `{{S1},{S2}}`;
16. artificial unique-winner rewrite of alternative-joint causality;
17. `non_polarized -> supports` laundering on an unresolved causal participant;
18. support/refute polarity inversion under stale terminal state;
19. causal/residual role movement;
20. same-ID proposition content substitution;
21. exact Contract-B bundle/world substitution;
22. evidence reference absent from the exact bound B world;
23. exact CAL semantic implementation substitution;
24. policy digest substitution;
25. policy-resolver authority substitution;
26. failed result-set laundering into completed epistemic state;
27. completed `not_checkable` laundering into failed/incomplete execution;
28. stale local `result_set_id` after semantic mutation;
29. coherent local reseal after deleting retained residual semantics;
30. attacker-selected replacement external digest after coherent reseal;
31. cross-proposition participant/basis substitution;
32. cross-world member composition under one B binding;
33. producer-private terminal branch/reason injection;
34. measurement/score semantic injection;
35. malformed terminal verdict/completion pairing;
36. fabricated terminal state on failed/incomplete proposition execution;
37. stable public reason substitution `MIXED_RELATIONS -> mixed_relations`.

Attack 37 must be rejected by RC1 even if an attacker recomputes local identity. Exact frozen-oracle comparison must also identify the lowercase object as semantically different.

## Seeded weak controls

The experiment must visibly distinguish four intentionally weak constructions from the required stack:

1. local-reseal-only verification accepts coherent retained-residual deletion that independent handoff authority rejects;
2. neutral-as-support consumer unsafely strengthens unresolved `not_checkable` to support;
3. covering-only basis checker accepts fake joint `{S1,S2}` over independent alternatives that the frozen oracle rejects;
4. a copied/case-normalized terminal-reason oracle accepts lowercase `mixed_relations`, while the verified frozen Phase 2 oracle retains exact `MIXED_RELATIONS` and rejects equality.

If weak control 4 cannot demonstrate the specific RC0 false-green mechanism, RC1 qualification is experiment-insensitive.

## Acceptance rule

Disposition is `QUALIFIED_FOR_CAL_PRODUCER_CONFORMANCE_RESEARCH` only if, with the frozen RC1 candidate bytes unchanged:

- all mandatory Phase 1/2 and RC1 blob anchors verify;
- 19/19 positive specimens reconstruct exactly from the directly consumed Phase 2 oracle;
- 5/5 canonical permutation controls preserve one canonical identity;
- 37/37 attacks are detected by an appropriate gate or become an explicitly different authorized/oracle object rather than silently equivalent state;
- 4/4 weak controls visibly expose their intended vulnerability while the required RC1 stack catches the corresponding attack;
- no false causal equivalence, arbitrary winner, neutral laundering, execution/epistemic laundering, replay/substitution, reason normalization, or boundary injection survives the full stack;
- `apparatus_failures=[]`;
- no post-freeze RC1 candidate repair occurs.

Otherwise disposition is `FALSIFIED_REQUIRES_SUCCESSOR_CANDIDATE` or `INCONCLUSIVE_EXPERIMENT_INSENSITIVE`, as appropriate.

## Nonclaims

This cohort is not CAL producer conformance, independent-consumer conformance, compatibility/SemVer analysis, release/promotion work, Decision policy, Contract E / Authorization, or execution authority.