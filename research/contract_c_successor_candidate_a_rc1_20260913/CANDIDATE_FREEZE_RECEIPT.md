# Candidate A RC1 freeze receipt

**Classification:** Draft Research / Research Infrastructure.

RC1 is an explicit successor to falsified RC0. It does not modify RC0 or the frozen Phase 0/1/1.5/2 records.

## Exact freeze

Candidate A RC1 is frozen at:

`ba5f55b0fb6ca54e0461a688813b66fd81bf8c4c`

Architecture authority remains Phase 2 freeze:

`f5337dedaad045aa290e80f69dc83ac1a0f73436`

Frozen RC1 blobs:

- `CANDIDATE_A_RC1.md`: `0ab62b7ceea1d56d6bc3b7cf8528769dd440ab7c`;
- `candidate_a_rc1.schema.json`: `0de83032e6d989225cf014d9a70035f634a5e003`;
- `candidate_a_rc1.py`: `03dbb5b774af523d8e6235bca86b18af5ddad5b0`.

The exact profile literal is `contract-c-successor-candidate-a-rc1-research`.

## Bounded successor delta

Relative to falsified RC0, RC1 preserves the Candidate A architecture and corrects the demonstrated exact-public-reason drift:

- RC0 incorrectly accepted `mixed_relations`;
- RC1 accepts the frozen programme identity `MIXED_RELATIONS` and rejects the lowercase replacement.

RC1 also makes exact Phase 1/2 oracle anchoring a qualification requirement, but that is an experiment-infrastructure rule rather than a new public wire surface.

No generic C1 assessment slot, rule/state causal namespace, repeated policy payload, repeated passage hash, separate contribution ID, measurement/score surface, Decision policy, Authorization state, or execution occurrence has been restored.

## No-patch rule

These three candidate blobs are immutable for the decisive RC1 adversarial cohort. Qualification infrastructure and result records may be added, but the candidate bytes may not change after this freeze.

If a revealed case requires changing any frozen RC1 candidate blob, RC1 is `FALSIFIED_REQUIRES_SUCCESSOR_CANDIDATE`; any repair belongs in a new successor candidate and new freeze.

## Qualification authority anchors

The decisive RC1 workflow must verify before execution:

- Phase 1 corpus blob `0ff4f66bca00924755d42ed7f944c4dae8d10f66` at `175246ae16932f2f34a399560d7f76013213bf97`;
- Phase 2 oracle blob `9f00cd332ad366b878f60d13440af639360928de` at `f5337dedaad045aa290e80f69dc83ac1a0f73436`;
- all three RC1 candidate blobs listed above.

The evaluator must consume the inherited Phase 2 oracle directly rather than reproducing its terminal semantic table.

## Boundary

This freeze is not production promotion, release, SemVer assignment, CAL producer conformance, independent-consumer conformance, a Decision Engine production change, Contract E / Authorization, or execution authority.